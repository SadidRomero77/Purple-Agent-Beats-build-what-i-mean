[project]
name = "agente-purpura-v2"
version = "0.1.0"
description = "Arquitectura base de Agente Purpura v2: teoria de juegos + filosofia de la mente"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
